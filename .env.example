# Vercel Blob token used by the /api/blob/sign endpoint to issue signed upload URLs.
# Get this from your Vercel project: Storage -> Blob -> .env.local snippet, or via
# `vercel env pull` after linking the project.
# When unset, the editor still runs locally; cloud Save / Open just disable themselves.
BLOB_READ_WRITE_TOKEN=
